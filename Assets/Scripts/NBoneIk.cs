using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NBoneIk : MonoBehaviour
{
    [SerializeField]
    private Transform m_startBone;

    [SerializeField]
    private Transform m_endBone;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
